# Bridge machine (openpilot + camera or screen capture)
opencv-python>=4.5
numpy
pynput
watchdog
mss>=9.0

# Companion machine (game + Fanatec wheel)
# Also needs: evdev (Linux only, for FFB and virtual pedals)
evdev>=1.6
