# Bridge machine (openpilot + camera or screen capture)
opencv-python>=4.5
numpy
pynput
watchdog
mss>=9.0

# Companion machine (game + virtual Xbox controller)
# Also needs: evdev (Linux only, for uinput Xbox controller)
evdev>=1.6
